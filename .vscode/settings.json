{
    "filewatcher.commands": [
        {
            "match": "\\.ts*",
            "isAsync": true,
            "cmd": "npx tsc ${file}",
            "event": "onFileChange"
        }        
    ]
}